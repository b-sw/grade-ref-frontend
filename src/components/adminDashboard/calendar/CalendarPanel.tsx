import {Match} from "../../../entities/Match";

interface Props {
  matches: Match[];
  readOnly?: boolean;
}

export const CalendarPanel = (props: Props) => {
  console.log(props);
  return (
    <>
    </>
  );
}